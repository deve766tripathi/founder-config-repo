spring:
  datasource:
    url: jdbc:mysql://localhost:3306/team_db
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    config-url: /teams/v3/api-docs

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: team-service:8085
